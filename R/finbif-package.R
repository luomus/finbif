#' @section Package options:
#' \describe{
#' \item{`finbif_api_url`}{Character. The base url of the API to query. Default:
#'   `"api.laji.fi"`}
#' \item{`finbif_api_version`}{Character. The API version to use. Default:
#'   `"v0"`}
#' \item{`finbif_use_cache`}{Logical. Should API queries by cached. Default:
#'   `TRUE`}
#' \item{`finbif_cache_path`}{The path to the directory where to store cached
#'   API queries. If unset (the default) in memory caching is used.}
#' \item{`finbif_tz`}{The timezone used by `finbif` functions that compute dates
#'   and times. Default: `Sys.timezone()`}
#'
#' }
"_PACKAGE"

# The following block is used by usethis to automatically manage
# roxygen namespace tags. Modify with care!
## usethis namespace: start
## usethis namespace: end
NULL
